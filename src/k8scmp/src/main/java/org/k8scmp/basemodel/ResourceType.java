package org.k8scmp.basemodel;

/**
 */
public enum ResourceType {
    APPLICATION,
    LOADBALANCER,
    PROJECT,
    CUSTOM,
    CLUSTER,
    SERVICE,
    ALARM,
    CONFIGURATION,
    GITLAB,
    LDAP,
    SUBVERSION;

}